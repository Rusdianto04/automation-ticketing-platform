name: deploy-production

on:
  push:
    branches: [ 'main', 'master' ]
    paths:
      - 'formbricks-discord-bot/**'

permissions:
  contents: read
  packages: write

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_OWNER:    ${{ github.repository_owner }}
  REPO_NAME:      ${{ github.event.repository.name }}

jobs:
  build-and-deploy:
    name: Build & Deploy to Production
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tags
        shell: bash
        run: |
          echo "FRONTEND_IMAGE=${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.REPO_NAME }}-frontend:latest" >> $GITHUB_ENV
          echo "BACKEND_IMAGE=${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.REPO_NAME }}-backend:latest"   >> $GITHUB_ENV

      - name: Build & push backend image
        uses: docker/build-push-action@v4
        with:
          context: ./formbricks-discord-bot/backend
          file: ./formbricks-discord-bot/backend/Dockerfile
          push: true
          tags: ${{ env.BACKEND_IMAGE }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push frontend image
        uses: docker/build-push-action@v4
        with:
          context: ./formbricks-discord-bot/frontend
          file: ./formbricks-discord-bot/frontend/Dockerfile
          push: true
          tags: ${{ env.FRONTEND_IMAGE }}
          cache-from: type=gha

      - name: Deploy to production server via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host:     ${{ secrets.PROD_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key:      ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            echo ">>> Pulling latest images..."
            docker pull ${{ env.FRONTEND_IMAGE }}
            docker pull ${{ env.BACKEND_IMAGE }}

            echo ">>> Deploying with docker-compose..."
            cd ~/deploy/formbricks-discord-bot
            FRONTEND_IMAGE=${{ env.FRONTEND_IMAGE }} \
            BACKEND_IMAGE=${{ env.BACKEND_IMAGE }} \
            docker compose up -d --remove-orphans

            echo ">>> Production deploy selesai."

      - name: Notif Discord — Production Sukses
        if: success()
        uses: ./.github/actions/discord-notify
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status:  success
          title:   "🚀 Production Deployed: ${{ github.repository }}"
          body: |
            **Branch:** `${{ github.ref_name }}`
            **Commit:** `${{ github.sha }}`
            **URL:** ${{ secrets.PROD_APP_URL }}
            **Actor:** ${{ github.actor }}

      - name: Notif Discord — Production Gagal
        if: failure()
        uses: ./.github/actions/discord-notify
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status:  failure
          title:   "❌ Production Deploy Gagal: ${{ github.repository }}"
          body: |
            **Branch:** `${{ github.ref_name }}`
            **Commit:** `${{ github.sha }}`
            **Logs:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
